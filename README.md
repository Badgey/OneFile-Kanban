# self-hosted-kanban-board
A self-hosted kanban board with swimlanes, packaged as a single HTML file. No install, no server, no dependencies, no accounts. Open it in a browser and use it. Data persists locally in IndexedDB. Drag and drop, markdown descriptions, checklists, JSON export.
